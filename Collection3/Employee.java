package Collection3;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;
public class Employee {
	public static void main(String[] args) {
		ArrayList<String>empName=new ArrayList<>();
		empName.add("ram");
		empName.add("vishnu");
		empName.add("ganesh");
		//empName.stream().map(x->x.toUpperCase()).forEach(z->System.out.println(z));
		//List<String>l=empName.stream().map(x->x.toUpperCase()).collect(Collectors.toList());
		//l.forEach(z->System.out.println(z));
		//for(String i:l) {
			//System.out.println(i);
		//}
		//empName.stream().map(y->y.length()>3).forEach(z->System.out.println(z));
		//List<Boolean>s=empName.stream().map(y->y.length()>3).collect(Collectors.toList());
		//s.forEach(a->System.out.println(a));
		//for(Boolean t:s) {
			//System.out.println(t);
		//empName.stream().map(x->x.charAt(x.length()-1)).forEach(z->System.out.println(z));
		//List<Character>l=empName.stream().map(x->x.charAt(x.length()-1)).collect(Collectors.toList());
		//l.forEach(z->System.out.println(z));
		//empName.stream().map(y->y.length()+10).forEach(z->System.out.println(z));
		//List<Integer>l=empName.stream().map(y->y.length()+10).collect(Collectors.toList());
		//l.forEach(z->System.out.println(z));
		//for(Integer s:l) {
			//System.out.println(s);
		//}
		//empName.stream().filter(x->x.contains("n")).forEach(z->System.out.println(z));
		//List<String> l=empName.stream().filter(x->x.contains("n")).collect(Collectors.toList());
		
		}
	}


